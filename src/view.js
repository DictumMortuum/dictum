// not node.js compatible.

"use strict";

(function() {
  var unit = function(path) {
    var api = {}

    return api
  }

  window.dictum = window.dictum || {}
  var logic = window.dictum.logic
  window.dictum.template = unit
})();
